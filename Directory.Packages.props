<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <!-- The FS.GG.Net.* packages release on their OWN version axis (bottom-layer sibling; mirrors
         $(FsGgAudioVersion) / $(FsGgGameVersion)). ADR-0052. -->
    <FsGgNetVersion>0.5.0</FsGgNetVersion>
  </PropertyGroup>
  <ItemGroup>
    <!-- The coordination kit as a versioned package (ADR-0062, .github#1262 / .github#1291).
         Referenced by .config/kit/FS.GG.Kit.receiver.proj, which materializes the kit onto disk;
         Renovate is MEANT to bump this pin, with kit-materialize.yml refreshing the committed kit on
         that PR. That arm is currently DEAD here (.github#1552): the org preset disables this whole
         FILE for every repo but FS-GG/.github, so no FS.GG.Kit PR is ever proposed for FS.GG.Net, and
         kit-materialize.yml — gated to renovate/* branches — never fires for its primary trigger.
         Until #1552 lands this pin must be bumped BY HAND and the materialize run explicitly:
             dotnet build .config/kit/FS.GG.Kit.receiver.proj -t:FsggKitMaterialize
         Skipping that is what red-lit `coordination-coherence` on main (#37). Do NOT re-enable the
         file with a local Renovate rule — that is a second dialect (.github#850); land #1552. -->
    <PackageVersion Include="FS.GG.Kit" Version="0.18.0" />

    <!-- FSharp.Core: the org common pin (Rendering/SDD/Governance/Audio all pin 10.1.301).
         FS.GG.Net.Core depends on FSharp.Core ALONE. -->
    <PackageVersion Include="FSharp.Core" Version="10.1.301" />

    <!-- Serialization: FS.GG.Net.Protobuf. Google.Protobuf is the reproducible raw-bytes path
         (ToByteArray / MessageParser) that SC2-over-WebSocket needs (ADR-0052 §4). protobuf-net +
         protobuf-net-fsharp are the code-first path for owned schemas (absorbed from fsGRPCSkills). -->
    <PackageVersion Include="Google.Protobuf" Version="3.29.3" />
    <!-- Grpc.Tools: build-only protobuf codegen, used by the samples/Sc2Handshake vendored-.proto lib. -->
    <PackageVersion Include="Grpc.Tools" Version="2.69.0" />
    <PackageVersion Include="protobuf-net" Version="3.2.45" />
    <PackageVersion Include="protobuf-net-fsharp" Version="0.1.0" />

    <!-- gRPC transport: FS.GG.Net.Grpc (the BAR substrate). Thin lifecycle wrapper only — grpc-dotnet
         and protobuf-net.Grpc already do method dispatch well; we do not re-abstract it. -->
    <PackageVersion Include="Grpc.Net.Client" Version="2.67.0" />
    <PackageVersion Include="protobuf-net.Grpc" Version="1.1.1" />
    <!-- Server hosting for the samples/GrpcService code-first service. -->
    <PackageVersion Include="protobuf-net.Grpc.AspNetCore" Version="1.1.1" />
    <PackageVersion Include="System.ServiceModel.Primitives" Version="8.1.0" />
    <!-- Overrides the vulnerable 8.0.2 that System.ServiceModel.Primitives pulls transitively (NU1903). -->
    <PackageVersion Include="System.Security.Cryptography.Xml" Version="10.0.10" />

    <!-- Elmish Cmd/Sub authoring surface: FS.GG.Net.Elmish. The standard Elmish library (never
         FS.GG.UI — the transport component stays render-independent, like FS.GG.Audio.Elmish). -->
    <PackageVersion Include="Elmish" Version="5.0.2" />

    <!-- Test-only. Pinned to the same versions the sibling FS-GG repos' ported suites use. -->
    <PackageVersion Include="Expecto" Version="11.1.0" />
    <PackageVersion Include="FsCheck" Version="3.3.4" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="YoloDev.Expecto.TestSdk" Version="0.16.0" />
  </ItemGroup>
</Project>
